name: whatsapp_share_improved
description: Fork from whatsapp_share with namespace and fix AGP 7+ and y embedding v2
version: 2.0.3
homepage: https://github.com/sebasIngSoft/whatsapp_share_improved

dependencies:
  flutter:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.example.whatsapp_share
    pluginClass: WhatsappShareImproved

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.20.0"