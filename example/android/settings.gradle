pluginManagement {
    // 1️⃣ Carga la ruta de Flutter SDK
    def flutterSdkPath = {
        def properties = new Properties()
        file("local.properties").withReader("UTF-8") { properties.load(it) }
        def flutterSdkPath = properties.getProperty("flutter.sdk")
        assert flutterSdkPath != null, "flutter.sdk not set in local.properties"
        return flutterSdkPath
    }()

    // 2️⃣ Incluye el build de Flutter Tools
    includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

    // 3️⃣ Repositorios para los plugins
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// 4️⃣ Plugin Loader y versiones de AGP/Kotlin
plugins {
    id "dev.flutter.flutter-plugin-loader" version "1.0.0"
    // Ajusta estas versiones según tu Android Gradle Plugin y Kotlin
    id "com.android.application" version "8.1.0" apply false
    id "org.jetbrains.kotlin.android" version "1.8.22" apply false
}

include ':app'
